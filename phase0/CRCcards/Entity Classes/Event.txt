Class name: CalendarEvent
Parent class: None
Child classes: OneOffEvent

Responsibilities:
* Keeps track of the name, time, and duration of the event

class CalendarEvent(cname, cstart, cend):
    self.cname = ...
    self.cstart = Time(cstart)
    self.cend = Time(cend
    self.duration = Time(cend - cstart)
